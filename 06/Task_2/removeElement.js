// const arr = ['one', 'two', 'three'];

function removeElement(index, item) {
    index = arr.indexOf(item);
    if (index > -1) {
      arr.splice(index, 1);
    }
}

// removeElement(1,'two');
// console.log (arr)

